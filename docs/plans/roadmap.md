---
name: "the roadmap: every open item, merged and ranked"
overview: "ONE FILE FOR ALL OPEN WORK, ranked so that each step SHIPS. Five plans held 43 pending items between them, with the same work described from different angles in different files: MoE residency appeared in freetoken-parity as policy and in amd-strix-halo as a UMA question, CPU attention appeared in llama-cpp-parity-push twice, and measurement discipline appeared in three. They are merged here by WHAT THE WORK IS rather than by which document happened to raise it. THE GOAL that ranks them is in `north-star.md`: be the Rust alternative to llama.cpp, same models, same command shapes, same or better performance, on the hardware people actually own. THE RULE THAT OVERRIDES THE RANKING is `ship-small-or-do-not-start`: shipped and working beats big and undone, so an item that cannot be cut into steps which each land is filed wrong. THE ORDER AND WHY IT CHANGED: correctness and the verification oracle come FIRST, ahead of the model layer refactor, for two reasons found on 2026-08-27. One, some checkpoints are wrong RIGHT NOW and the fixes are small: `smollm3` is one line to refuse, the byte-fallback is seven call sites. Two, the BPE defect stayed invisible for the life of the project precisely BECAUSE the only cross-engine oracle hands llama.cpp explicit token ids and so never tests the tokenizer. Refactoring 15464 lines across two crates before closing that hole means refactoring known-wrong code without a way to tell if you broke anything. TWO ITEMS HAVE THEIR OWN DESIGN DOCUMENT and are referenced rather than expanded: `model-layer-reorg.md` and `out-of-core-moe.md`. THE HONEST POSITION, re-counted 2026-09-02: of 150 architecture catalog rows, 16 run with evidence, 4 dedicated engines load without cross-engine evidence, 41 refuse as unaudited with the missing piece TRIAGED and named, and 90 refuse by name or route to one of those 4 engines. The LOADS-AND-IS-WRONG class is CLOSED: the generic path is opt-in, so an unaudited architecture stops instead of guessing. Metal is at or past parity, 8 of 12 decode rows faster than llama.cpp. CPU is behind on all 16 comparable rows. Superseded plan files are archived, not deleted."
todos:
  - id: ship-small-or-do-not-start
    content: "NOT A TASK, the rule that overrides theme order when the two disagree. SHIPPED AND WORKING BEATS BIG AND UNDONE. Any item here that cannot be cut into steps which each LAND and each leave the engine better is filed wrong, and the fix is to re-cut it rather than to start it. Two consequences that actually bite. FIRST, the model layer reorg is the largest item in this file and ships nothing by itself: it is therefore cut into an audit that ships a correctness fix per finding, then a shared block vocabulary proven on ONE path, then architectures moved one at a time, never a big-bang rewrite of 15464 lines across two crates. This project has already lost work to a branch whose copy of a file predated three others. SECOND, an item whose first honest milestone is 'does it load at all' says so instead of promising a tok/s number: out-of-core against a 155 GB checkpoint is a research bet, not a delivery, and it is ranked as one. THE TEST BEFORE STARTING ANYTHING: name what a user can do after step one that they could not do before. If the answer is 'nothing yet, but later', cut it smaller."
    status: note

  - id: a0-dissolve-frink-edge
    content: "LANDED on `wf/dissolve-edge`, recorded here because what it FOUND outlives what it moved. `frink-edge` was FreeToken's host-side policy meant to be INTEGRATED into frink; it became a separate 28384-line crate that was roughly half wired. It is gone. The serving half is `frink-server::policy`, the MoE expert-residency half is in `frink-core` BESIDE `expert_store` so there is ONE expert byte budget rather than two -- on unified memory two budgets are the same RAM counted twice, which is `out-of-core-moe.md`'s `one-budget-not-two`, now structurally impossible to get wrong. `frink-cuda` lost its only frink dependency and is a leaf again; `CudaExpertPool` moved to frink-core because frink-core already depends on frink-cuda and the reverse edge would have been a cycle, which also puts it where the Metal pool belongs beside it. DELETED, 8098 lines: the multi-currency paged prefix cache (`cache_manager`, `radix::swa`, `radix::hybrid`, `window_pool`, `state_pool`), which is a SECOND page allocator for a job `frink_core::cache::KvBlockPool` already does on a wired, running path; `cache_report`, a renderer for a geometry nothing built; `supervisor`, the lifecycle of an engine CHILD PROCESS frink never spawns; the async detokenizer worker; the recurrent-state half of `anchor`. THE METHOD MATTERS MORE THAN THE NUMBER: module-level reachability was WRONG both ways. It said `stats`, `maintenance`, `outbox`, `footprint` and `rebuild` had zero external callers when in fact every one of them is wired -- the callers went through the crate's `pub use` facade, so a `frink_edge::module::` grep missed them. And it said `window_pool` and `state_pool` were reachable, via `scheduler` functions that were themselves dead. WHAT ACTUALLY WORKS is making the code crate-INTERNAL and reading `dead_code`: a published crate's `pub` items are invisible to that lint, which is precisely why 5400 lines could sit there tested and uncalled. Every module keeping an unwired half now NAMES the roadmap item that would close it, at its declaration in `policy/mod.rs`; `grep -n 'allow(dead_code)' crates/frink-server/src/policy/mod.rs` is the standing to-do list."
    status: done

  - id: a0b-ideas-kept-from-the-deleted-policy
    content: "NOT A TASK YET, the ideas from `a0-dissolve-frink-edge`'s 8098 deleted lines that are worth NOT re-deriving. Each is good; each had an implementation that did not fit frink's shape, which is a different verdict from 'wrong'. (1) THE PREFIX-CACHE HANDOVER TRAP, and the one worth reading first. When a request offers a computed prefix to the radix tree, the span the tree ALREADY had is duplicates the request must free, and `InsertResult::cached_len` is deliberately documented as 'how much you must free' rather than 'how much I stored' -- read the other way it leaks a page per concurrent duplicate, forever, and the symptom is a pool that fills under load with nobody holding it. Freeing is ALSO not enough: the request is still READING those positions, so its page table must be REPOINTED at the tree's canonical pages first. CHECKED AGAINST THE LIVE PATH ON THIS BRANCH: `generate::publish_to_radix` has NEITHER defect, and avoids both structurally rather than by handling them -- pages are refcounted groups, the request keeps its own duplicates until its own `Drop`, and only `[cached_len, inserted_len)` gets the extra `retain_group`. Keep the rule written down anyway; the next implementation of this will not be refcounted by accident. (2) THE SWA TOMBSTONE RULE, which is a silent-corruption class and has no equivalent in frink today: a window request's out-of-window positions still hold FULL KV, so their pages publish normally, but their WINDOW slots are gone -- the tree must TOMBSTONE below that frontier rather than adopt them as window-live, or a later request matches the node and gathers through a mapping that reads the reserved sentinel. Slot 0's bytes in every window head, on a path that never errors. This matters the moment frink gives sliding-window models a shared prefix cache. (3) THE FOUR SUPERVISOR RULES, if frink ever grows a daemon that spawns an engine: a retried start must not become a second engine (two on one GPU is OOM at best, two serving different weights on one port at worst); ONE party reaps and everyone else waits on what it publishes, because a poll TRANSIENTLY LIES while the monitor holds the lock; the stop-requested latch is set when the stop is REQUESTED, not when it completes, or a crash mid-stop reads as an unplanned death and the supervisor restarts what you were stopping; and re-adoption keys on `(pid, start_time, argv, port)` because PIDs recycle and `start_time` is the only part a recycled PID cannot reproduce. (4) DEFERRED FREES ACROSS A BATCH: within one batch several requests commit, and a page freed by the first must not reach the third while the second is still reading it. (5) An exact-TOKEN-length prompt for a serving benchmark needs a FIXED-POINT loop, because decode-then-encode is not the identity -- the tokenizer merges adjacent pieces, so a prompt built from N ids re-encodes SHORTER and a run labelled '512-token prompts' quietly benchmarks something else. Frink's own `bench -p 512` is unaffected (it tokenizes in-process); this bites any HTTP-side serving benchmark."
    status: note

  - id: a1-expert-streaming-is-wrong-on-real-checkpoints
    content: "SILENTLY WRONG, FOUND 2026-08-28, and it invalidates the headline capability rather than merely delaying it. Expert streaming produces GARBAGE on a real checkpoint. Reproduced deterministically at temperature 0 on OLMoE-1B-7B Q4_0: resident answers ' Paris.' to 'The capital of France is', streamed with FRINK_EXPERT_CACHE_BYTES=134217728 answers ' amongst amongst, and of'. Both stable across runs, so it is not nondeterminism. WHY NOBODY SAW IT: `store_backed_experts_produce_bit_identical_logits_to_resident` passes, including at a 1-BYTE budget where every acquire misses, so the fixture does not exercise whatever differs. The fixture is a small synthetic MoE; OLMoE is Q4_0 with `packed_q4` planes, and `packed_q4` is built ONLY for `ExpertBacking::Resident` (loader.rs:1923), so a streamed layer takes a different execution path entirely. That is the first place to look. CONSEQUENCE, and it is why this is ranked above everything: `out-of-core-moe.md` is the strongest single reason to choose frink over llama.cpp, and its foundation returns wrong tokens. Nothing downstream of it can be trusted until this is fixed, including any measurement of it. AUTO-ENABLING IS NOW REFUSED in both binaries: a model that does not fit reports that it does not fit and names streaming as an explicit opt-in, rather than quietly serving nonsense. Explicit FRINK_EXPERT_CACHE_BYTES still works, for somebody deliberately debugging this. DONE MEANS: streamed and resident produce identical ids on OLMoE and on one shared-expert MoE, proven on hardware, and the fixture is extended until it would have caught this. RE-MEASURED 2026-08-31, AND IT NO LONGER REPRODUCES: streamed and resident are BYTE-IDENTICAL on OLMoE across four diverse prompts, both through the extended `crates/frink-models/tests/streaming_bytes.rs` and through a live server on `/v1/completions`. The fixture was widened from one prompt to four for exactly the reason the original slipped through -- a deterministic bug that one prompt happens to miss is a bug the test cannot see. AUTO-ENABLE STAYS WITHDRAWN ANYWAY. The output agrees; nothing establishes WHAT fixed it, and restoring an automatic behaviour on 'it works now, cause unknown' is how the original shipped. What is owed before restoring it is the second checkpoint: one shared-expert MoE, which OLMoE is not, and which is the shape `metal_prefill_moe` explicitly refuses."
    status: pending

  - id: b1-silently-wrong-today
    content: "THEME B, HIGHEST VALUE IN THE WHOLE ROADMAP, because these produce fluent wrong answers rather than errors. All found by the 2026-08-27 audit. (1) `smollm3` ROTATES NINE LAYERS llama.cpp does not: `src/models/smollm3.cpp:5` sets `n_no_rope_layer_step = 4` with NO GGUF key, so the file loads clean and runs at full speed while rotating every layer. Same class as the gpt2/mpt/refact/bloom/jais bug already fixed, on a more popular checkpoint. Fix is a per-layer rope skip plus a registry entry, or one line to refuse. (2) ONE HARDCODED BPE PRE-TOKENIZER REGEX serves EVERY BPE checkpoint (`tokenizer.rs:308-311`); zero of llama.cpp's ~30 variants exist. Any number of 4+ digits and any run of 2+ whitespace tokenizes differently, so indented code and blank lines diverge from llama.cpp on Llama-3.x, Qwen, DeepSeek and SmolLM. Invisible because BPE tests are round-trip only and `frink parity` hands llama.cpp explicit ids, excluding the tokenizer from the only cross-engine oracle. CHEAPEST FIRST MOVE is goldens from llama.cpp's CI corpus, not the regexes. THE ORACLE'S HOLE IS NOW CLOSED, 2026-08-31: `frink parity` tokenizes a 19-case corpus through BOTH engines on the same GGUF and diffs the ids, each case aimed at a CLAUSE that differs between llama.cpp's pre-tokenizer regexes rather than at prose. It went red on its FIRST real run and found four more defects `pretokenize_regex_for` did not close: `deepseek-r1-qwen` has no arm and falls to the GPT-2 default where upstream maps it to QWEN2; `gpt-4o` is aliased onto qwen2 where upstream has its own pattern, so every multi-digit number tokenizes one digit at a time; `should_add_bos_token` misses the `llama-bpe` group, so a raw Llama-3.2 prompt is short one BOS; and worst, `encode_text_run` does `find_iter().flat_map()`, which SILENTLY DISCARDS the text between matches -- on the `olmo` arm, whose regex has no trailing `\\s+`, tabs and NBSP and interior newlines vanish from the prompt. That last one is data loss on the input, not a segmentation difference. ALL FOUR FIXED THE SAME DAY, and the sweep now MATCHES llama.cpp on every checkpoint it can load: Llama-3.2, DeepSeek-R1-Distill (was 7/19), OLMoE (was 5/19), Phi-4-mini (was 7/19), Qwen2.5, TinyLlama, gemma-2, Yi-1.5, Mistral-7B. No allow-list and no corpus edit. Patterns now live in `tokenizer/pretokenize.rs`. TWO MORE FOUND WHILE TRANSCRIBING, neither of which the corpus caught: the qwen2 arm spelled its contractions lowercase-only where upstream executes `(?:'[sS]|'[tT]|...)`, invisible on `DON'T` because the letter clause matches `'T` either way; and the `olmo` and `gpt-2` arms were TWO COPIES of one upstream string varied by a trailing `|\\s+` llama.cpp does not have, of which only the olmo copy was ever exercised. STILL OPEN AND DOCUMENTED IN `regex_for` RATHER THAN GUESSED: the DeepSeek family, `superbpe`, `minerva-7b`, `tiny-aya`, `poro-chat` and `viking` each need a SEQUENCE of regexes applied in order, which one `Regex` cannot express and which no local checkpoint can prove; `jina-v2-code` is on the qwen2 arm where upstream says GPT2; and gemma-4 forces add-BOS over an explicit `false`, which the oracle cannot check because libllama will not load that file. (3) An unhandled `tokenizer.ggml.model` FALLS BACK TO BYTES (`frink-server/src/model.rs:146-151` plus four CLI clones) so bert/rwkv/none vocabs generate garbage instead of refusing. (4) `rope.scaling.type = linear` is silently ignored (`loader.rs:733-736`) with a TEST asserting it. (5) `default_swa_pattern` covers 6 archs where llama.cpp hardcodes 18, so `mellum` and `exaone-moe` window every layer, `smallthinker` diverges four ways at once, and `gemma3n` is transcribed as 6 where llama.cpp says 5. STATE 2026-08-31, verified against the code rather than against this entry: (1) CLOSED, `smollm3` refuses by name in `capability.rs`. (2) CLOSED, `pretokenize_regex_for` picks a per-family regex over `fancy_regex` for the llama3/qwen2/olmo/gpt2 groups. (3) CLOSED, an unhandled `tokenizer.ggml.model` refuses by name in `frink-server/src/model.rs` and in the CLI clones instead of falling back to bytes. (4) CLOSED, `linear_scaling_from_gguf` reads it. (5) CLOSED 2026-08-31. The table is 17, every architecture llama.cpp seeds a scalar `swa_period` for, each with a source citation and pinned by `tests/swa_pattern.rs`. Finishing it needed a SECOND field rather than more rows: `set_swa_pattern` has two phases, and `ModelConfig` knew one. `dense_first=true` puts the full-attention layer at `il % p == 0`, not `il % p == p-1`, so `smallthinker` and `laguna` -- both LIVE on generic GQA -- could not be given their period at all without putting 16 layers of a 32-layer model on the wrong span. `ModelConfig::swa_dense_first` now carries it and both phases are implemented. Also found and fixed while there: `pattern = 1` was filtered out of the metadata and fell through to the per-arch default, where llama.cpp means NO layer windowed and frink did every layer -- exactly inverted, and a config test asserted the two spellings were the same. ALL FIVE OF THIS ITEM ARE NOW CLOSED."
    status: done

  - id: b1c-template-sniffers-replaced-by-rendering
    content: "LANDED, and it closes the last substring match against template SOURCE anywhere in the serving path. The end-of-turn marker had an arm per family -- `<start_of_turn>`, `<|im_end|>`, `<|turn>` -- so it answered for three families and answered NOTHING for every other, which on a checkpoint whose turn marker is not its EOS means the marker is EMITTED AS TEXT and the model talks past the end of its own answer (Yi-1.5, observed: 71 tokens instead of 7). It is now DERIVED, in `frink-server/src/policy/turn.rs`, by rendering three conversations and diffing: the gap between an assistant turn and the user turn that follows is the terminator plus the next turn's opener, and the opener render supplies the second half. Same mechanism as the tool and effort probes, which is the point -- what a template PRINTS is the question and its source is a proxy that answers a different one. PROVEN ON 17 REAL CHECKPOINTS, nine families, none of them named in the code: `<|im_end|>`, `<end_of_turn>`, `<turn|>`, `<|eot_id|>`, `</s>`, `<|end|>`, and DeepSeek's `<\u{ff5c}end\u{2581}of\u{2581}sentence\u{ff5c}>` whose FULLWIDTH pipes no `<|...|>` pattern matches. TWO BUGS THE SYNTHETIC TESTS COULD NOT HAVE FOUND, both caught by running it against `models/`. One, Llama-3 emits a default system block, so its opener already contains an `<|eot_id|>` and a longest-common-suffix strip ran back through the one that was the answer and consumed the whole gap; fixed by splitting on what closes the LAST user turn, where nothing follows to be confused with it. Two, that same common-suffix strip matched `s>[INST] ` on Mistral and reported `</`, and `</s>\n<|user|>\n` on TinyLlama the same way -- a TRUNCATED marker is worse than none, because no marker leaves EOS to stop the run while a truncated one claims something already does. Fixed by stripping the opener EXACTLY, after taking off the BOS only a first turn carries. THE RULE THIS PAYS FOR: a probe validated only against templates the test file wrote is validated against nothing. `cargo test -p frink-server --lib policy::turn -- --include-ignored`."
    status: done

  - id: g-verification-without-downloading-everything
    content: "THEME G, cross-cutting, and the answer to 'do we have to test every model'. No. 140 real checkpoints is tens of terabytes and would still only prove the ones downloaded. THREE MECHANISMS ALREADY EXIST IN THIS REPO, each applied to far less than it could be. (1) DIFFERENTIAL, the decisive one: `frink parity` plus `tools/llama_logits.c` compares first-token logit distributions against llama.cpp on the same file and needs no knowledge of the architecture. It is NOT run across the suite, and it currently EXCLUDES the tokenizer by handing llama.cpp explicit ids, which is precisely why the BPE defect in `b1` went unseen. (2) SYNTHETIC FIXTURES: `scripts/make_dots1_fixture.py` and `make_gptoss_fixture.py`, two architectures out of 139. A fixture is kilobytes, needs no download, and catches wrong tensor wiring and wrong refusals. One per supported architecture is the best coverage-per-byte available and should be a REQUIREMENT of the registry in THEME A. (3) PINNED PROPERTY TABLES transcribed from llama.cpp's own loader: `LLAMA_NO_ROPE` does this for one property and is exactly what caught the gpt2/bloom class. Extend to ALiBi slopes, norm placement, QKV bias and the MoE routing bias."
    status: pending

  - id: a-model-layer-scalability
    content: "THEME A, THE GATE ON EVERYTHING ELSE. `crates/frink-models/src/decoder.rs` is 6438 lines and holds the generic path plus per-architecture branching, so adding a model means editing it. llama.cpp has 140 architectures because adding one is a small new file; this project has 150 catalog rows of which 16 are proven. The counts differ for a structural reason. It already cost correctness: the paged decode loop had LOST five model features (`attention_scale`, `post_attn_norm`, `post_ffn_norm`, gpt-oss `o_bias`, `gpt_oss_ffn`) by being a copy of the contiguous one, three wrong on CPU too. DESIGN IS IN `model-layer-reorg.md`. Reference shapes studied: llama.cpp's `src/models/*.cpp` (one file per arch building a ggml graph) and `.scratch/FreeToken/python/freetoken/models/` (a PACKAGE per arch with its own config parser, weight iterator, attention and ops, plus a `register.py` keyed by architecture string). FreeToken is the closer fit, and this project already ported its host-side policy in, now spread across frink-core and frink-server. MEASURED CAVEAT from FreeToken's own `support-muse-glimmer` branch: adding an architecture there touched 27 files and `support-minimax-m3` touched 49, because a new model brings a tool-call parser, a reasoning parser, effort vocabulary and server metadata, not just a graph. Isolating the decoder is necessary and NOT sufficient. Frink is already better placed on that axis, since the ported parser owns 11 tool-call formats and probes effort from the template."
    status: pending

  - id: b2-close-the-68
    content: "THEME B. 68 architectures shared ONE generic GQA path when this was written; the manifest counts 57 today, as rows left it for named refusals. Against llama.cpp's bespoke graphs, five were found silently wrong. THE CHEAP FIX BEATS THE AUDIT: make the generic path OPT-IN. Today an unrecognised architecture FALLS ONTO generic GQA and runs; it should have to ASSERT it is plain GQA. That inversion turns 63 unaudited architectures from silently-wrong into honestly-refused, which is this project's stated rule everywhere else, and it makes the remaining work visible instead of hypothetical. It also depends on the registry from THEME A, which is why that is first. THEN audit outward from what people actually run: `qwen3moe`, `glm4moe`, `deepseek2` (MLA), `minimax`, `gemma3`/`gemma4`, `dots1`, `olmoe`. The 2023 tail IS in scope under this goal (same models as llama.cpp includes `bloom` and `gpt2`) but comes last and is handled safely meanwhile by the inversion. Absorbs `coverage-cheap-archs`. STATE 2026-08-31: THE INVERSION ALREADY LANDED -- an unrecognised architecture refuses with `UnauditedArchitecture` and must be in `AUDITED_GENERIC_GQA` to run, and `an_unaudited_generic_architecture_refuses_rather_than_guessing` pins it. THE AUDIT OUTWARD IS UNDER WAY, by synthetic fixture rather than by download, which is `g`'s mechanism 2 and costs kilobytes per architecture. `qwen3moe` AUDITED against llama.cpp's own graph through libllama on all three forward paths (per-head QK norm before RoPE, `head_dim * n_head != n_embd`, GQA, NEOX, renormalised softmax top-k, `n_ff != n_ff_exp`). TWO REAL BUGS FOUND, both the SAME SHAPE and neither about the graph: a loader demanding a hyper-parameter the architecture is not supposed to have, and an error naming the demand rather than the mismatch. (1) `glm4moe` -- GLM-4.5 / 4.5-Air / 4.6 -- was dispatched by BOTH binaries to the GLM-5.2 MLA loader, which asks for `q_lora_rank`; glm4-moe.cpp builds plain Q/K/V and reads none of the four MLA keys, so a real GLM-4.5-Air download failed with 'missing hparam glm4moe.attention.q_lora_rank'. Now refuses on the generic path naming the ONE thing genuinely missing, a norm slot that is gpt-oss's exactly and is already implemented behind an `is_gpt_oss` flag. (2) `deepseek2` -- DeepSeek-V2/V2.5/V3/R1, the largest open models people run -- required `attention.qk_nope_head_dim` and `.qk_rope_head_dim`, and NEITHER IS A GGUF KEY: both are HF `config.json` field names absent from `LLM_KV_NAMES` and from `gguf-py`. Invisible because the loader's own tests built fixtures supplying the invented keys. Now derived as llama.cpp does it from `key_length_mla`, `value_length_mla` and `rope.dimension_count`, with the plausible wrong fix (`attention.key_length`, which holds the COMPRESSED MQA width) pinned as unequal so the mix-up cannot be silent. `gemma4` CONTRACT-CHECKED against the real checkpoint (split 512/256 head dims, per-layer KV reuse, the per-layer `feed_forward_length` ARRAY a scalar read gets wrong for 20 of 35 layers, and `f_attention_scale = 1.0` -- Gemma-4 applies NO `1/sqrt(head_dim)`, a factor of 22.6 on every full-attention score); NOT audited and deliberately OFF the list, because the installed libllama predates gemma4 and there is no cross-engine evidence to be had. `minimax` REFUSAL REASON WAS WRONG, the fourth instance of the pattern: it blamed MTP, which no MiniMax GGUF can contain (`gguf-py`'s tensor list has no `NEXTN_*` entry, so the writer physically cannot emit one), and it blamed sigmoid MoE routing that `loader.rs` already implements. It also treated M2 and M3 as one architecture when only M3 is block-sparse. Now two rows: `minimax-m2` is UNAUDITED rather than unimplemented -- plain GQA, whole-vector Q/K norm, partial NEOX RoPE, sigmoid MoE with router bias, all of which frink has -- and `minimax-m3`'s real blocker is MSA's per-layer indexer and its own KV cache. THE PATTERN IS NOW THE FINDING: four architectures audited, THREE had a refusal or a loader naming something the architecture is not supposed to have, and every one stayed invisible because frink's own fixtures supplied the invented keys. A fixture transcribed from llama.cpp's CONVERTER, not from frink's loader, is what breaks that. STILL OPEN: earning `qwen3moe`'s evidence standard for `minimax-m2`, which needs a libllama logit comparison on the new fixture. STATE 2026-09-01, the triage step landed: all 46 unaudited refusals now carry a VERDICT naming what is missing and the `llama.cpp/src/models/*.cpp` line that decides it -- 9 fixture-away, 2 one-match-arm, 26 new-code, 4 unknown, after five one-match-arm rows were closed on 2026-09-02 -- pinned by `crates/frink-models/tests/unaudited_triage.rs`, with `TRIAGE_PENDING` empty. THE COUNT WENT 47 TO 46 FOR THE RIGHT REASON: reading `src/models/minicpm3.cpp:5-6,41-46` showed `minicpm3` requires `q_lora_rank`/`kv_lora_rank` and the DeepSeek-2 tensor set, so it was never a generic-GQA model and now refuses by name. TWO FINDINGS WORTH MORE THAN THE CLASSIFICATION. The ALIAS TRIO: `mistral`, `mixtral` and `yi` are in neither llama.cpp's `LLM_ARCH_NAMES` nor gguf-py's, real checkpoints all convert to `llama`, and the catalog gives all three NEOX RoPE while `llama` is in the NORM group -- a file actually spelling `mistral` would be rotated on the wrong pairs of every Q/K head. Latent only because the row refuses. And the SIGMOID-GATING DEFAULT turned up three more times (`afmoe`, `laguna`, `step35` default to sigmoid when `expert_gating_func` is absent, against frink's softmax fallback and a list of two), which is the `deepseek` shape again: a default right for most architectures, silently wrong for one, with NO GGUF KEY to correct it. WHAT REMAINS is executing the queue the triage produced."
    status: pending

  - id: c-out-of-core-big-models
    content: "THEME C, the strongest reason to choose frink over llama.cpp rather than tie with it. Run a model larger than memory: DeepSeek-V4-Flash is 155 GB at UD-Q4_K_XL against a 32 GiB machine. DESIGN IS IN `out-of-core-moe.md`. WHAT EXISTS: `frink-core` holds the policy already (`expert_cache` LRU with copy plans, `expert_slots` bounded pool behind a `SlotDevice` trait, `placement`, `residency`, `footprint`, `pool`) and NOTHING EXECUTES IT except `expert_pool::CudaExpertPool`, compile-verified only with its hardware test `#[ignore]`d. Needs a `SlotDevice` for Metal and one for host RAM, plus an SSD-backed source. REFERENCE: `.scratch/ds4` does this in C, with `ds4_ssd.c`, `ds4_layer_pack.c` for an on-disk layout that keeps the next-needed bytes contiguous, and `ds4_streaming_hotlist.inc`, a PRECOMPUTED PROFILED expert hotlist of 13334 {layer, expert} pairs sorted by hits/weight, shipped as a header per model family. THAT LAST IDEA MATTERS HERE SPECIFICALLY: frink tries to learn hotness at runtime via `activation_counts`, and that signal is ALL ZEROS ON METAL (`frink-metal/src/attn.rs:5620` returns empty id vectors; `MoeDecodeScratch.ids` is one top_k buffer reused per layer). So frink's eviction policy currently reads nothing on the backend this machine uses. Absorbs `persistent-gpu-expert-cache`, `concurrent-cpu-moe-executor`, `double-buffered-prefill`, `uma-residency-semantics` and `real-moe-checkpoints`."
    status: pending

  - id: h-a-large-real-checkpoint
    content: "THEME H, the thing that would prove THEME C is real. Research on 2026-08-27 found ONE recent large model that clears both gates: DeepSeek-V4-Flash-0731, `general.architecture` = `deepseek4`, 304B total / ~13B active, 256 routed experts plus 1 shared, 155 GB at UD-Q4_K_XL. ggml-org PUBLISHES THE GGUF ITSELF and llama.cpp merged support on 2026-06-29 (#24162), so a baseline exists. Its shape is worth the run: MLA, a lightning indexer, per-layer compression alternating 4 and 128, mHC hyper-connections, a routing bias tensor (`exp_probs_b`), and `scoring_func: sqrtsoftplus`. The checkpoint is NATIVELY FP4, so any Q4_K_M is a requantization of already-4-bit weights, which is why bartowski ships MXFP4 only. WHAT WAS REJECTED AND WHY, so it is not re-researched: Qwen3.8-Flash-Next exists (2026-08-26, unsloth GGUF) but llama.cpp support is an OPEN PR written before the weights were public, so there is NO BASELINE, and 36 of its 48 layers are Gated DeltaNet, which lands in frink's unconditional `HybridEngine::reject`. Kimi K3 is linear attention on 69 of 93 layers, GLM-5.3-Flash is 34 of 45, both in the same reject family. GLM-5.2, MiniMax-M3 and Muse Glimmer clear the gates but are less interesting or much larger. NOTE `docs/MODELS.md` says DeepSeek V4 is 'loaders and primitives only, nothing run end to end on a real checkpoint', and the suite tops out at Mixtral/48 GB, so the honest first milestone is DOES IT LOAD, not a tok/s number."
    status: pending

  - id: c1b-streaming-and-fused-metal-moe-are-structurally-exclusive
    content: "MEASURED 2026-08-28, and it corrects an earlier reading of this codebase. I had recorded that expert streaming 'gives up the fused Metal MoE kernels' because ten sites match `ExpertBacking::Resident` and only two had been opened, implying the other eight were an oversight to remove one at a time. They are not. THE REAL CONSTRAINT: the fused Metal MoE decode stack needs `moe.packed_q4`, a single contiguous buffer holding EVERY expert, and `try_build_moe_packed_q4_planes` is called only for `ExpertBacking::Resident` (loader.rs:1923), so a streamed model has `packed_q4: None`. The `Resident` guard at decoder.rs:1844 is therefore REDUNDANT with the `moe_packed_q4` check immediately after it: deleting the guard would change nothing, because the pack is absent anyway. You cannot pack what you do not hold, so packing all experts is the exact opposite of streaming them. WHAT THIS MEANS FOR THE ROADMAP: having both is not a gate-opening exercise, it is the Metal `SlotDevice` from `out-of-core-moe.md`, where the top-k experts for a token are gathered into a BOUNDED GPU pool and the kernel is pointed at that pool rather than at a whole-model pack. That is the same work item, and this is one more argument for it rather than a separate task. Until it lands, turning streaming on means the CPU MoE path, and the two Resident gates I did open (decoder.rs:1182 and :1255, the `try_metal_moe_topk` fallback) are the only ones that were ever removable. NOT A REGRESSION and not urgent: streaming only turns itself on when the weights do not fit, and a model that does not fit was not running at all before."
    status: done

  - id: c2-moe-execution-quality
    content: "THEME C. Recent good models are overwhelmingly MoE and MoE is where this engine is weakest. (1) FIX THE METAL `activation_counts` DROP, which is a prerequisite for THEME C's eviction policy, not a separate curiosity: widen `MoeDecodeScratch.ids` to `top_k * n_layers` and slice it, or add a persistent per-expert histogram the router increments atomically, which preserves the no-sync intent the current comment cites. (2) `concurrent-cpu-moe-executor`: q* assumes a step costs max(fetch, cpu) and frink's CPU MoE does not overlap them. (3) `double-buffered-prefill`: not portable as policy alone, it needs a copy stream and two events per buffer. (4) OLMoE CPU is 2.19x prefill and 2.46x decode while its Metal rows are at parity. (5) MoE bench coverage rested on ONE model until Qwen1.5-MoE was added, which has SHARED experts, a shape OLMoE lacks and one `metal_prefill_moe` explicitly refuses. (1) IS CLOSED 2026-08-31, and the finding is worse than 'the signal is missing': the ONE wired consumer, `PlacementPlan::plan_layers_against_global_budget`, is on all four forward paths and ON BY DEFAULT on Metal, and with every count zero its sort collapses to layer-ascending -- so GPU residency slots went to layer 0's experts 0,1,2 until the budget ran out and every later layer got NONE. A confident plan built from nothing, and `None` and `Some(all zeros)` are bit-identical so the `has_observations` gate never fired. Two causes: `launch_moe_decode_stack` hard-coded empty vectors citing a sync tax that was ALREADY PAID (the readback sits after the existing `waitUntilCompleted`), and `MoeDecodeScratch.ids` was one `top_k` buffer bound at offset 0 by every layer, so only the last layer's selection survived. Fixed by a `layers x stride` buffer with a slot per layer, in `frink-metal/src/moe_ids.rs`. THREE FOLLOW-UPS FOUND WHILE FIXING IT, none closed: (a) the fused Metal PREFILL path drops routing the same way -- `encode_moe_prefill_ffn` writes `route_ids` into a per-layer-reused scratch nothing reads -- and closing it needs a `decoder.rs` signature change, since the prefill stack returns only hidden state; `layer_divergence.rs:676` already pins the gap. (b) `activation_counts` is NEVER reset or decayed, and `residency_plan` is rebuilt on EVERY forward call, so now that the signal moves, early tokens permanently dominate the ordering for the life of the process -- a THEME C policy question and the next thing that will bite. (c) `decoder.rs:988` claims expert hotness for `inspect-plan` comes from decode, but `inspect-plan` is header-only and never runs a forward pass."
    status: pending

  - id: e-cpu-performance
    content: "THEME E, ranked below correctness because it makes NO new model runnable, though under the north star's bar it is a genuine parity failure and cannot be dropped. Every red row is CPU: all 16 comparable rows, prefill 1.41x to 5.06x, decode 1.68x to 3.55x. MEASURED EVIDENCE THAT REDIRECTS THE WORK: the gap scales INVERSELY with model size, 5.06x at 135M down to 1.41x at 7B, which is the signature of FIXED PER-MATMUL OVERHEAD rather than kernel throughput, and every i8mm kernel already exists and is on the hot path. So `cpu-decode-scaling` (fork-join, against llama's persistent spin-barrier pool) is the remaining lever and the kernel items are largely spent. `cpu-kill-transpose` stays open but its 7 serial scatter loops are already gone; what remains is a 32-float stack tile whose fix needs an aliasing raw pointer into frink-quant, judged not worth the unsafe on no profile. FIRST EVIDENCE THE RECENT WORK PAID: TinyLlama CPU decode 48.11 to 54.41 tok/s, gap 2.15x to 2.00x, and prefill 2.02x to 1.97x."
    status: pending

  - id: d-hardware-reach
    content: "THEME D, and the largest single hardware gap. Frink has CPU, Metal and CUDA: Apple and NVIDIA and NOTHING ELSE. Every AMD and Intel GPU has no path, while llama.cpp reaches all of them through Vulkan, so 'same hardware as llama.cpp' is currently false by a wide margin. RE-SCOPE REQUIRED: the four Vulkan items are filed as blocked on owning a Strix Halo box, which is the wrong framing. Vulkan is testable on ANY machine with a Vulkan driver including an Intel iGPU; Strix Halo is a tuning target, not a prerequisite. ORDER: `backend-seam-refactor` first, because there is no backend trait at all today and it is a prerequisite for any third backend; then `vulkan-beachhead` as a GO/NO-GO on the smallest honest slice (`ash` plus one SPIR-V kernel), not a full backend; then decode, then prefill GEMM, which is the item that decides whether the whole thing was worth doing. Absorbs `hip-revisit-gate`, which stays REJECTED unless all three of its conditions hold, and `gtt-carveout-doc`. STATE 2026-09-01, and it belongs here because it is the same seam argument: a batched quantized GEMM landed for CUDA (`frink-cuda::mul_mm`, Q8_0 and Q4_0), ported from `frink-metal`'s `mul_mm_sg_impl` seam and unpack rather than its simdgroup intrinsics. IT HAS NEVER EXECUTED ON A GPU. Its evidence is a thread-by-thread scalar twin held against `frink_quant`'s independent dequantize-then-GEMM, plus a host harness that compiles and RUNS the emitted CUDA against a `std::thread` barrier shim, zero mismatches on both kinds at three shapes. The hardware test is `#[ignore]`d with NEVER RUN as its reason and `docs/` claims no CUDA GEMM. Two things learned and worth keeping for the Vulkan work: NVRTC is not clang, so 'it compiles here' is not 'the runtime compiler accepts it'; and a real GPU contracts to FMA, which is why the ignored hardware test compares at 1e-4 relative rather than exactly."
    status: pending

  - id: d2-x86
    content: "THEME D. Ten items are doable on any x86 host and unblock a real class of commercial edge machine. `x86-first-measurement` is the blocker: there is NO frink measurement on any x86_64 host at all. `scripts/check-cross-target.sh` already gates `x86_64-apple-darwin` compilation in about 24 seconds, so `avx512-int-dot` and `strict-kernels-on-x86` can be WRITTEN and compile-checked here, held to the standard the existing AVX2 arms are held to: a verified algebraic mirror of the NEON arm, stated in the code as unrun. `avx512-int-dot` is called out as the largest lever with no new backend. `thread-default-x86` matters because `default_worker_threads()` reads `hw.perflevel0.physicalcpu` on macOS and falls through elsewhere. `docs-and-features-honesty` belongs at the top of this theme in spirit: it exists to stop the project claiming a backend nobody has measured, the same rule that keeps CUDA at 'must compile'."
    status: pending

  - id: b3-the-embedding-model-gap
    content: "THEME B, and the LARGEST RAW CHECKPOINT-COUNT GAP in the audit, larger than any decoder family. Frink handles 3 of llama.cpp's 6 `tokenizer.ggml.model` values (gpt2/gemma4 to BPE, llama to SPM, t5 to Unigram). MISSING: `bert`/wpm with no WordPiece code anywhere, `rwkv` with no trie tokenizer, and `none`. Separately, 12 encoder/embedding architectures (`bert`, `nomic-bert`, `jina-bert-v2/v3`, `modern-bert`, `gemma-embedding`, `llama-embed`) all refuse, and `/v1/embeddings` only pools a DECODER GGUF's hidden states. So every BGE, E5, nomic-embed, jina-embed and GTE, which is what people actually use for embeddings, cannot be served. There is also no `/v1/rerank` route and `classifier.output_labels`/`pooling_type` are unread. Medium to large, and it unlocks more published files than anything else here."
    status: pending

  - id: b4-quant-and-doc-correctness
    content: "THEME B, small and cheap. (1) UNKNOWN GGUF TYPES SIZE AS ZERO: `block_layout` returns `(0, 1)` for `Other(_)` (`frink-gguf/src/lib.rs:230`) so `byte_len` short-circuits to 0 and `tensor_bytes` returns an EMPTY SLICE WITH NO ERROR. A TQ2_0 file does not fail to parse, it under-counts every size estimate then fails later somewhere less informative. (2) MXFP4 is accepted as a weight matrix (`loader.rs:1172`) and an MoE expert (`:1220`) but REJECTED by `load_f32_vec` (`:1124`) despite `dequant_mxfp4_gguf` existing, so an MXFP4 norm or bias is a hard load error. One missing match arm. (3) Genuinely missing live ggml types: TQ1_0/TQ2_0 ternary, NVFP4, Q1_0, Q2_0. The `Q4_0_4_4` family is missing but REMOVED from ggml, so it is not a gap. (4) `docs/MODELS.md:157-159` is wrong in BOTH directions at once: IQ4_XS DOES have a Metal matvec and simdgroup GEMM and IQ4_NL/IQ4_XS both have NEON kernels, while 'scalar + AVX2' overstates the second IQ tier. (5) A Q5_0 Metal GEMM exists but Q5_0 is excluded from `metal_mul_mm_kind_supported`, so Q5_0 MoE experts run on Metal while Q5_0 dense weights fall to CPU. STATE 2026-08-31, each verified against the code: (1) was already closed end to end and merely untested; now guarded by a real GGUF built with tag 250. (2) was NOT closed and the audit named the wrong line -- `widen_plain_float` already had its MXFP4 arm, but `load_f32_vec` routed only `F32|F16|BF16` into it, so MXFP4 fell past twenty quant arms to `UnsupportedDtype`. Fixed. (3) DONE as recognized-and-SIZED named refusals for TQ1_0/TQ2_0/NVFP4/Q1_0/Q2_0, layouts read off ggml's structs; deliberately NOT implemented, because a ternary dequantizer with no golden vector is how you ship a format that loads and is wrong. (4) the two lines flagged were ACCURATE; the real defect was a third SIMD tier the binary split hid -- `IQ1_S`/`IQ2_XXS`/`IQ3_XXS` are AVX2-or-scalar with no NEON, so on Apple they are the scalar tier. Replaced with a per-format tier table. (5) left refused, and its rationale was backwards: Q5_0 dense PREFILL already runs on Metal via `mul_mm_sg_launch`, which never consults that table; DECODE is what falls to CPU, for want of a `q5_0_matvec`. FOUND WHILE AUDITING THIS, and worse than any of the five: `use_mm_id` picked the fused Metal MoE path on batch size ALONE, while `mul_mm_id_meta` knows only Q4_0/Q8_0/Q4_K and the loader packs seven kinds. A Q5_K, Q6_K or IQ4_XS MoE checkpoint therefore ran fine until one expert gathered eight tokens and then died with a bare `CommandFailed` naming nothing. Now falls back to `mul_mv_id`, which handles all seven. STATE 2026-09-01: (5) IS NOW CLOSED, in the direction the corrected rationale pointed. `Q5_0_MATVEC_KERNEL_SRC` exists, `metal_matvec_kind_name` and `metal_mul_mm_kind_supported` name the same seven kinds, and Q5_0 decode no longer falls to the CPU while its prefill runs on the GPU. CORRECT BY CONSTRUCTION AND UNMEASURED: there is no Q5_0 checkpoint in `benchmarks/suite.json`, so the bench row that would prove it is still owed. `docs/MODELS.md`'s tier table moved Q5_0 up beside IQ4_XS to match."
    status: done

  - id: c3-serving-and-kv
    content: "THEME C, the serving half. (1) `sched-time-debt`: time-debt prefill/decode interleaving, where chunk DURATION is the quantum because a GPU cannot preempt a running kernel. Read that todo's own text, it carries the design. (2) `wire-radix-prefix-cache` and `window-slide-during-decode`: partial, and the policy half of the window slide is done with a caller (`anchor::decode_slide`). (3) `child-log-stream`: deliberately not built, because frink has no consumer for it yet. (4) `bench-bw`: the writer exists (`bench_profile::entry_from`); the reader does not. NOTE the batcher defect wave 0 found and could not confirm: continuous batching prefills with `forward_token`, which leaves the host cache SHORT rather than zeroed, and whether the batcher calls the existing catch-up before `forward_multi_seq` was never established. TWO MORE FOUND BY `a0-dissolve-frink-edge`, both in the LIVE radix path and both operator-visible. (a) `RadixCache::evict` HAS NO CALLER outside its own tests, so nothing ever releases the reference `generate.rs:685` takes when a request publishes. Every finished request permanently donates its published pages to the tree and the free pool shrinks MONOTONICALLY until `acquire_group` returns `None` and admission starts refusing. `docs/ROADMAP.md` already knew (`back pressure today is the page store running out`); what is new is that it is the whole eviction path, not a tuning knob. (b) `batch_scheduler` ADOPTS from the tree (`acquire_paged_caches`, `batch_scheduler.rs:1562`) but NEVER calls `publish_to_radix`, so under `FRINK_CONTINUOUS_BATCHING=1` prefix sharing is adopt-only against a tree nothing fills. Both are cheap and both are `wire-radix-prefix-cache`. STATE 2026-08-31, verified against the code: (a) CLOSED, eviction is wired into the acquire retry loop in `generate.rs`. (b) WAS ALREADY FALSE when written -- `Rows::flush_finished` calls `publish_to_radix` and `into_decode_start` carries `prompt_ids` through the prefill-to-decode handover, both of which landed with the `batch_scheduler.rs` split. What was actually missing was the EVIDENCE: the one test asserted `total_size() > 0`, which a tree handing back the WRONG pages would also pass. Now proven by page identity and by a pool sized so an unevicted tree starves."
    status: pending

  - id: f-measurement-and-proof
    content: "THEME F, cross-cutting, and it now blocks the CLAIM rather than merely annoying us. Every number comes from ONE 32 GiB M2 Pro that is also the development machine. Already hit: two Metal rows skipped mid-suite when macOS CoreSuggestions spiked the load, a whole suite lost to an orphaned process holding the instance lock, Mixtral permanently unmeasurable, CUDA never measured, x86 never executed. LANDED 2026-08-27: a receipt `host_spec` field recording CPU model, core split, RAM and OS but NOT hostname or user, and a render that REFUSES to merge receipts from different hosts. STILL NEEDED: (a) `suite-validate-every-change` and `quality-gates`, where a row closes only at gap <= 1.0x AND answers match llama.cpp; (b) `close-all-red-rows`, the definition of done for parity; (c) `tooling-quality-eval`, since frink validates NUMERICS against NumPy goldens but cannot answer whether a quantization damaged the model; (d) `ci-x86-and-vulkan`, since CI builds cuda twice and metal once and runs GPU tests never; (e) `hygiene-cross-target-gate`, half landed, with the `rust-toolchain.toml` pin still owed; (f) a BARE-METAL rental, NOT a shared-vCPU instance, because a noisy neighbour's steal time does not appear in the guest's load average and would defeat the quiet-host guard invisibly."
    status: pending
isProject: false
---

# The roadmap

Every open item, merged by what the work is rather than by which
document raised it. The goal is in [`north-star.md`](north-star.md).

**The rule that overrides the order: shipped and working beats big and
undone.** An item that cannot be cut into steps which each land, and
each leave the engine better, is filed wrong. Before starting anything,
name what a user can do after step one that they could not do before.
If the answer is "nothing yet, but later", cut it smaller.

**What `status` means here.** `pending` is open work. `done` is landed
AND verified against the code, never against this file's own claim: a
merged PR once marked `paged-decode-path` complete while it returned
wrong tokens on Metal. `note` is a rule or a finding that is worth not
re-deriving and is not a task. An item can be `pending` with most of its
content closed, and when it is, the content says which parts.

Two items are big enough to have their own design document:

- [`model-layer-reorg.md`](model-layer-reorg.md), splitting the
  decoder so architectures scale. It was 6438 lines when that document
  was written and is 6702 today, the first time it has shrunk.
- [`out-of-core-moe.md`](out-of-core-moe.md), running a 155 GB model on
  a 32 GB machine.

## Order, and what each step ships

| # | Theme | Ships what |
|---|---|---|
| **1** | Fix what is wrong, and close the oracle's hole | Correct output on models that run today. Small, immediate |
| **2** | Model layer: audit, vocabulary, then split | A correctness fix per audit finding, then one architecture at a time |
| **3** | Close the 41: **inversion done, triage done**, five closed, now audit outward | Honest refusals instead of silent wrong answers, each naming what is missing |
| **4** | Out-of-core MoE, and one large real checkpoint | A model that did not fit, running. First milestone is "does it load" |
| **5** | CPU decode scaling | One item carrying most of the remaining CPU gap |
| **6** | Hardware reach: backend seam, then Vulkan | Every AMD and Intel GPU, currently none |
| **7** | The rest: embeddings, quants, serving, measurement | Breadth, once the above hold |

## What was merged

Five plans held 43 pending items, with the same work described from
different angles in different files. MoE residency appeared in
`freetoken-parity` as policy and in `amd-strix-halo` as a UMA question.
CPU attention appeared twice in `llama-cpp-parity-push`. Measurement
discipline appeared in three files.

One item was already stale and is now closed:
`publish-order-blocked-on-workflow-scope` said `frink-edge` was missing
from the crates.io publish order. It was added, and all 13 crates
published at 0.12.0 and again at 0.13.0.

Superseded plan files moved to [`archive/`](archive/). They are kept
because their reasoning is worth not re-deriving, not because they are
still the plan.

## The rules that keep being re-learned

**A plan's own status field is a claim, not evidence.** A merged PR once
marked `paged-decode-path` complete while it returned wrong tokens on
Metal.

**One agent owns a file.** Two branches editing one file produce a merge
nobody can review, and this project has had a branch silently revert
three others.

**No agent runs benchmarks.** A loaded host reads 25-45% low.

**Refusing is not a defect.** llama.cpp will often run something
approximately; this project stops and names what is missing.
