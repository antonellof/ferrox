---
name: "model layer reorg: make adding an architecture a new file"
overview: "THE PLAN north-star's `t1-scale-the-model-layer` forward-references as `model-registry-reorg.md`, which was never written. THE PROBLEM, counted: `crates/frink-models/src/decoder.rs` is 6438 lines, of which lines 1 to 4842 are code and 4843 to 6438 are its own tests. Four host functions inside it each contain a complete decoder layer written out longhand: `forward_token` (decoder.rs:1582, CPU arm at 2394-2531), `forward_token_paged` (2585), `forward_hidden_batch_inner` (3785, itself two arms), `forward_multi_seq_kv` (4625). `crates/frink-metal/src/attn.rs` is 9026 lines and contains SEVEN more, one per fused launch: `launch_decode_dense_layer` (5657), `launch_decode_dense_stack` (5928), `launch_moe_decode_layer_fused` (5311), `launch_moe_decode_stack` (5439), `launch_prefill_dense_layer` (7082), `launch_prefill_dense_stack` (6890), `launch_prefill_attn_block` (7238). So the same one decoder layer is spelled out roughly ELEVEN times across two crates, and every optional feature has to be remembered in all eleven. `post_ffn_norm` alone is applied at seven separate sites inside `forward_hidden_batch_inner`. THE EVIDENCE THIS COSTS CORRECTNESS is already on the record: the paged decode loop had LOST FIVE model features (`attention_scale`, `post_attn_norm`, `post_ffn_norm`, gpt-oss `o_bias`, `gpt_oss_ffn`) by being a copy of the contiguous one, three of them wrong on CPU too, and PR #22 fixed the same shape from the other side by giving `forward_multi_seq` a `MultiSeqKv` parameter rather than growing a paged twin (commit c5aa646, decoder.rs:444-453). A SIXTH GAP WAS FOUND WHILE WRITING THIS PLAN and it is the argument in miniature: `attention_scale` is applied at decoder.rs:2459 and decoder.rs:2699 and NOWHERE ELSE, so the batched prefill path and the multi-sequence path do not apply it. It is unreachable today only because `loader.rs:458` hardcodes `let attention_scale = None;` and `unsupported_scaling_keys` refuses a file carrying a non-noop `*.attention.scale`. The guard test that exists, `the_paged_path_keeps_every_per_layer_feature_the_contiguous_one_applies` (decoder.rs:5249), sets `attention_scale = Some(0.37)` and compares the two paths that AGREE, never the two that do not. Nothing notices, which is exactly the failure mode. WHAT THE REFERENCES SAY. Two shapes are on the table and they are not in conflict. FreeToken's `python/freetoken/models/` is a PACKAGE PER ARCHITECTURE: `llama/` is 5 files and 280 lines (`__init__.py` 5, `config.py` 48, `weight.py` 62, `attention.py` 80, `model.py` 85), while `deepseek_v4/` is 10 files and 2039 lines and grows its own `attention.py`, `compress.py`, `ops.py` and `moe.py` WITHOUT touching anything shared. Registration is one row in `models/register.py`: a frozen `ModelSpec { module, model_cls, parse_config, iter_weights }` whose last two default but can be overridden, which is how `Gemma4GGUFForCausalLM` reuses the gemma4 graph with a GGUF front end. llama.cpp's `src/models/` is 140 .cpp files totalling 32,514 lines, 136 of them one architecture each, median 150 to 250 lines, six of them SIX lines. Its per-file unit is a class with three pure virtuals (`load_arch_hparams`, `load_arch_tensors`, `build_arch_graph`, llama-model.h:747-749) invoked from exactly three places (llama-model.cpp:1233, :1360, :2413). THE VERDICT, argued in `the-shape-decision`: take the PACKAGE and the REGISTRY ROW from FreeToken, because frink executes eagerly and has no graph object for `build_arch_graph` to return, and because frink already accepted FreeToken's design once in `frink-edge`; take the OVERLOADED ATTENTION BLOCK from llama.cpp, because frink's actual bug is that its KV backing varies and llama.cpp overloads `build_attn` on the memory-input type seven ways (llama-graph.h:1145 through :1249) for precisely that reason, and PR #22 reached the same conclusion independently. THE CONSTRAINT THAT SHAPES EVERY STEP: Metal is at parity and 8 of 12 decode rows are faster than llama.cpp, and `launch_decode_dense_stack` folding every layer into one command buffer is what those rows rest on. No step may put dispatch inside that. THE ORDER, and it matters: shared vocabulary FIRST, split SECOND. oxillama is the same language and the same problem and it split per-arch WITHOUT block-level helpers, and it pays 54,853 lines across 26 architectures, about 2,100 each, because every arch inlines its own attention loop (oxillama-arch/src/llama/model.rs:332, ~187 lines). llama.cpp with `build_attn`/`build_ffn` pays about 230. FreeToken with shared OP objects pays 280. Splitting frink before the vocabulary exists lands on oxillama's number."
todos:
  - id: the-shape-decision
    content: "THE ANSWER TO 'what is the per-architecture unit, concretely, in frink terms', and it is TWO TIERS because frink already has two and both are load-bearing. TIER A, the common case, is a MODULE `crates/frink-models/src/arch/<name>.rs` holding one zero-sized type that implements a three-method `Arch` trait, named after llama.cpp's three virtuals because the mapping is 1:1 and a reviewer benefits from the correspondence: `hparams(&self, &ShardedGguf, &mut ModelConfig)` (llama.cpp `load_arch_hparams`, llama-model.h:747; FreeToken `parse_config`, models/register.py `ModelSpec.parse_config`), `tensors(&self, &ShardedGguf, layer, &mut LayerBuild)` (llama.cpp `load_arch_tensors`, llama-model.h:748; FreeToken `iter_weights`), and `layer_plan(&self, &ModelConfig, layer, &LayerWeights) -> Result<LayerPlan, LoadError>`. The third one is where frink MUST diverge from llama.cpp and follow FreeToken instead. llama.cpp's `build_arch_graph` returns a `unique_ptr<llm_graph_context>` holding a ggml graph, which `llama_context::process_ubatch` then CACHES and reuses across tokens (`llm_graph_result::can_reuse`, llama-graph.cpp:1378; `build_graph` called at exactly one site, llama-context.cpp:1354). Frink has no graph object and executes eagerly, so there is nothing to amortise a per-token virtual call over and no `build_arch_graph` to port. `layer_plan` therefore returns DATA built once at load, which the one shared runner reads. TIER B, for a genuinely different graph, is a module implementing the EXISTING `Engine` trait (engine.rs:243-252). That tier already exists and works: `kimi_decoder.rs` (1037), `glm52_decoder.rs` (436), `deepseek_v4_decoder.rs` (922), `gemma4_engine.rs` (485), `MlaEngine` (engine.rs:346), plus five refusal stubs (`minimax_engine.rs` 58, `llama4_engine.rs` 43, `hybrid_engine.rs` 68, `recurrent_engine.rs` 37, `t5_engine.rs` 34). This plan proposes NO change to Tier B's shape and says so deliberately, because the instruction is to extend what exists rather than invent a parallel mechanism. Note honestly what Tier B does NOT get today: `Engine` carries only `forward_token`, so a Tier-B architecture has no batched prefill, no paged KV and no prefix cache. `frink-server::generate` takes `&Decoder` concretely (generate.rs:200, :307, :388, :741) and `generate_engine` (generate.rs:1203) is the degraded fallback loop. Widening `Engine` is real work and is listed in `not-in-this-plan` rather than smuggled in here."
    status: pending
  - id: decoration-audit
    content: "STEP 0, and it is the cheapest thing in this plan: a TABLE, one row per optional layer feature, one column per layer body, saying applied or not. It exists because that is how the sixth gap was found, by grepping one feature across four paths and noticing `attention_scale` appears at decoder.rs:2459 and decoder.rs:2699 and nowhere else. Nobody has done that for the other features, so the honest statement today is 'at least six lost, count unknown'. The columns are the four host bodies (`forward_token` CPU arm decoder.rs:2394-2531, `forward_token_paged` 2585, `forward_hidden_batch_inner` 3785, `forward_multi_seq_kv` 4625) and the seven Metal launches (frink-metal/src/attn.rs:5311, :5439, :5657, :5928, :6890, :7082, :7238). The rows are at minimum: `q_bias`/`k_bias`/`v_bias`, `q_norm`/`k_norm` and which of the two `QkNormStyle` widths, `rope_attn_factor`, per-layer `rope_theta` (config.rs:392), partial rotary `rope_dim`, `attention_scale`, `layer_sliding_window` (config.rs:346), `attn_logit_softcap`, gpt-oss `attn_sinks`, gpt-oss `o_bias`, `post_attn_norm`, `post_ffn_norm`, `ffn_activation` GeGLU vs SwiGLU vs fused, `gpt_oss_ffn` routing bias and clamped OAI SwiGLU, `embedding_scale`, `final_logit_softcap`. THE OUTPUT IS NOT A DOCUMENT, it is the field list of `LayerPlan` in the next step, plus a bug count. Anything the table shows missing from a reachable path is a bug to file immediately, not to wait for the refactor. Anything unreachable today, like `attention_scale` behind `loader.rs:458`, is recorded as latent with the fence that keeps it unreachable named, because the fence is what a later plan will remove. `layer_needs_metal_stack` (decoder.rs:877) is the same table written by hand for five features and it is the reason this matters on the device side: a sixth feature added to the host path with no matching line there is a silent wrong answer on Metal, not a fallback. DONE 2026-09-01. THE COUNT RESOLVES: the plan said \"at least six lost, count unknown\"; it is EIGHT. THE COLUMN LIST CHANGED WHILE AUDITING IT -- four host bodies became THREE, because `s1-one-attn-block` collapsed `forward_token`'s CPU arm and `forward_token_paged` onto one `attn_block` whose only parameter is `KvStep`. And the Metal side is NINE launches reached from `decoder.rs`, not seven. ONE REACHABLE BUG, and it is the same failure mode at a fourth site: four places decide whether a MoE layer may use the GPU router, each carried its own hand-copied `.is_none()` list, and the copies had DRIFTED THREE WAYS -- prefill checked all three routing conditions, `launch_moe_decode_layer_fused` checked two, `launch_moe_decode_stack` checked NONE, and `launch_moe_decode_pre` re-routed on the host with bare `route_top_k` instead of `route_for_layer`. `exp_probs_bias`, `expert_weights_scale` and `expert_group_count` all come from real GGUF metadata with no loader fence, so a checkpoint carrying any of them routed to different experts with different weights depending on which path served the token. Fixed by one `gpu_router_matches_host_routing` (commit `916b096`). ONE LATENT GAP FOUND: GeGLU is not implemented for ROUTED MoE experts -- `run_ffn_block` branches on `ffn_activation` only in its dense arm, and every routed path is unconditionally SwiGLU. Uniform across all bodies, so a missing feature rather than a drift; fenced because `loader.rs` sets `Gelu` only for GemmaFamily and every GemmaFamily row on `GenericGqa` is dense. ONE HAZARD WITH NO FENCE: `final_logit_softcap` is skipped when lm_head folds into the Metal stack, safe ONLY by monotonicity -- `out_launch` is `Some` only under greedy argmax, and `sc * tanh(x/sc)` is monotone, so the argmax is invariant. The day that path returns full logits instead of an id, Gemma-2's 30.0 cap silently stops applying. `attention_scale` remains dead data behind THREE layers, of which `loader.rs`'s unconditional `let attention_scale = None` is the one a later plan removes. Everything else verified as reaching every host column, most through shared helpers a fourth caller cannot forget."
    status: done
  - id: shared-vocabulary
    content: "THE ANSWER TO 'what shared vocabulary do the per-arch files call, and does frink already have it'. HALF. Frink has a good OP vocabulary and NO BLOCK vocabulary, and that distinction is the whole difference between llama.cpp's 230 lines per architecture and oxillama's 2,100. PRESENT, in `frink-core` (lib.rs:27-55): `rms_norm`, `rms_norm_per_head`, `swiglu`, `geglu`, `gelu`, `silu`, `situ_and_mul`, `softcap_inplace`, the whole `causal_gqa_attention` family (`_softcap`, `_windowed`, `_windowed_softcap`, `_sinks`, `_paged`, `_paged_sinks`, `_prefill`, `_prefill_shared_kv`, `_prefill_shared_kv_windowed`), `apply_rope_with_freq_factors` and `apply_rope_interleaved*`; and in `frink-moe` (lib.rs): `route_top_k`, `route_top_k_biased`, `route_top_k_grouped_biased`, `route_top_k_sigmoid_with_bias`, `route_top_k_sqrtsoftplus`, `route_hash`, `route_gemma4_moe`, `run_expert`, `run_expert_placed`, `run_expert_oai`, `swiglu_oai`, `combine_expert_outputs`. ABSENT, with exactly one exception: there is no helper that runs a whole ATTENTION BLOCK. The one block-level helper that exists is `Decoder::run_ffn_block` (decoder.rs:3416), and that is the tell, because it is the one part of the layer the paged path did NOT lose. Everything else in the layer, the QKV projection and its GPU-multi arm, the three biases, the two QK norms, `rope_attn_factor`, the per-head RoPE loop, the `attention_scale` compensation, the cache push, the three-way attention arm select at decoder.rs:2468, `o_proj`, `o_bias`, `post_attn_norm`, is written out inline in every one of the eleven copies. WHAT TO BUILD, and it comes from llama.cpp rather than FreeToken: `attn_block`, OVERLOADED ON THE KV BACKING. llama.cpp overloads `build_attn` seven ways on the memory-input type, with an identical remaining signature each time, and pairs each overload with a factory that does the `static_cast` once so the block never re-casts (declarations llama-graph.h:1145, :1161, :1177, :1195, :1215, :1233, :1249; factories llama-graph.cpp:2634, :2736, :2840, :3197, :3256, :3288, :3138). Frink has the same problem for the same reason and has already reached the same answer once: `MultiSeqKv` (decoder.rs:444-453) exists precisely so that paging the per-sequence attend step is a PARAMETER rather than a second copy of a 300-line function, and its doc comment says so. FreeToken does NOT provide this and cannot, because torch SDPA already owns the KV write, so its per-arch `attention.py` is free to be 80 lines. Frink has no such luxury. WHAT TO TAKE FROM FREETOKEN INSTEAD is the discipline that a variant is a TYPE, not a flag: `layers/norm.py` exports `RMSNorm`, `RMSNormFused`, `GemmaRMSNorm`, `GemmaPlusOneRMSNorm`, `GemmaPlusOneRMSNormFused` as five named classes rather than one norm with two bools, and `layers/moe.py` exports `MoELayer` and `OffloadMoELayer` chosen at construction rather than branched in the hot path, which maps onto frink's existing `ExpertBacking::{Resident, Stored}` (decoder.rs:102). A named variant cannot be silently forgotten by a tenth copy the way a bool can."
    status: pending
  - id: s1-one-attn-block
    content: "STEP 1, the first thing to land, and it is a MOVE rather than a rewrite: net line count should be negative. Extract `attn_block(&self, layer_idx, &LayerWeights, rows, &mut KvBacking) -> Vec<f32>` covering everything from the QKV projection to `post_attn_norm`, and collapse `forward_token`'s CPU arm (decoder.rs:2394-2531) and `forward_token_paged` (2585-2792) onto it. `forward_multi_seq_kv` (4625) joins by widening the `MultiSeqKv` enum it already takes, which is what PR #22 built it for. `forward_hidden_batch_inner` (3785) is DELIBERATELY LEFT ALONE in this step: its rows are consecutive positions rather than independent sequences and its Metal arm set differs, and forcing it in would make step 1 unreviewable, which is the failure mode this repo has already paid for. SIZE: three call sites, one new function, no new types, no loader change, no capability change. VERIFIED BY tests that already exist and already pass, so a red one is a real regression rather than a new assertion: `forward_token_paged_matches_forward_token_bit_identical` (decoder.rs:5169), `every_paged_attention_arm_is_bit_identical_to_its_contiguous_twin` (5185), `the_paged_path_keeps_every_per_layer_feature_the_contiguous_one_applies` (5249), `the_paged_path_scales_embeddings_and_softcaps_logits_like_the_contiguous_one` (5323), `paged_prefill_is_bit_identical_across_every_arm` (5426), `a_paged_multi_seq_step_is_bit_identical_to_the_contiguous_one` (5869), plus `forward_batch_matches_sequential_forward_token_exactly` (5763) as the independent ground truth. Plus one `frink bench` receipt on Metal `tg128` and CPU `tg128` for `tinyllama_q8` and `llama32_1b_q4km`, both already in `benchmarks/suite.json` and both already on disk. SAY THIS PLAINLY rather than pretending otherwise: after this step those parity tests become close to tautological, because there is only one body left to compare against itself. That is the POINT. Their value moves from 'the two copies agree' into the type system, and they stay as a guard against a future re-split. Sabotage-check each one anyway, the way commit c5aa646 did ('dropping the window in the paged arm fails it, and so does dropping the softcap'), because a test that cannot fail is not a guard."
    status: pending
  - id: s2-layer-plan
    content: "STEP 2: `LayerPlan`, one per layer, built ONCE at load, stored on `LayerWeights` (decoder.rs:327), read by the shared block helpers instead of re-derived from `ModelConfig` plus a dozen `Option` fields on every token. Its field list is the output of `decoration-audit`. THIS IS NOT A NEW MECHANISM, it is finishing one that is already half-built and currently dead: `ExecutionPlan` (execution_plan.rs) is the model-level half, its doc comment already states the rule ('Selected once per model... Hot-path forward never re-derives architecture strings or fused-op availability'), it is constructed at decoder.rs:647 and loader.rs:1942, it is stored at decoder.rs:414, and it is READ BY NOTHING. Grep confirms: outside its own module the only mentions are the `pub mod` and `pub use` in lib.rs, the two constructions, and the field declaration. So `attention_scale` already has two homes, `ModelConfig::attention_scale` (config.rs:277) and `ExecutionPlan::attention_scale` (execution_plan.rs:73), and the prefill path consults neither. Fold `ExecutionPlan` in rather than adding a third. WHERE THIS EARNS ITS KEEP is the device side, not the host side. The three Metal eligibility predicates are hand-maintained lists of `.is_some()` checks: `layer_supports_metal_attn` (decoder.rs:819), `layer_needs_metal_stack` (decoder.rs:877, five features named by hand), `layer_supports_metal_dense_ffn` (decoder.rs:945). Deriving them from a `LayerPlan` the compiler forces you to handle exhaustively removes the class of bug where a feature lands on the host path and no matching line appears in the eligibility predicate, which is not a fallback but a wrong answer. DO NOT CLAIM A SPEEDUP. A `LayerPlan` field read replaces `self.config.layer_sliding_window(l)`, which recomputes a modulo per layer per token (config.rs:346), and replaces one `match` with another `match` of the same arity. That is a wash and it must be shown to be a wash with a receipt, not asserted. VERIFIED BY every existing decoder test staying bit-identical, plus a `LayerPlan` dump added to `frink inspect-plan` (frink-cli/src/main.rs:683) golden-filed against each fixture, which is kilobytes and needs no download, plus a test asserting the plan is stable across load and reload for every checkpoint in `benchmarks/suite.json`."
    status: pending
  - id: s3-arch-trait-and-gpt-oss
    content: "STEP 3: `trait Arch`, the `arch/` module, and exactly TWO movers, `arch/plain_gqa.rs` and `arch/gpt_oss.rs`. gpt-oss is the right first real mover and the reasons are all already in the tree. Its side table already exists (`GptOssWeights`, decoder.rs:359) and its doc comment (decoder.rs:333-346) already argues the exact design this plan generalises, in its own words: held as a side table rather than as five independent `Option`s because 'a checkpoint either has the whole gpt-oss graph or none of it, so `Decoder::gpt_oss.is_some()` is a single, checkable predicate', and 'scattering five independent Options would make half the graph is wired representable, and that state is precisely the silent-wrong-answer bug this work exists to remove'. Its hparams branch is already one `if arch == 'gpt-oss'` (loader.rs:1605) and its tensor loading is already one contiguous block (loader.rs:816-849), so `hparams` and `tensors` are lifts rather than designs. And it is the best-verified architecture in the repo: a 95 KB fixture (`crates/frink-models/tests/fixtures/gptoss_tiny.gguf`), a graph test (`crates/frink-models/tests/gptoss_graph.rs`), a generator (`scripts/make_gptoss_fixture.py`) and an INDEPENDENT reference implementation (`scripts/gptoss_reference_logits.cpp`). Landing it deletes the `gpt_oss: Option<GptOssWeights>` field and the `oai` local from all four host bodies. THE TRANSITIONAL SHAPE IS STOLEN FROM OXILLAMA, which is the one thing that project does better than either other reference: `oxillama-runtime/src/engine/mod.rs:1238` keeps a `build_forward_pass_direct` match over seven architectures that runs ONLY when the trait method returns `NotSupported`, with a comment at :1218-1227 saying each arm disappears as its owner overrides the trait, and a test at `engine/tests.rs:834` pinning the old behaviour. Frink should do exactly that: the inline gpt-oss branches stay as a fallback that no longer executes, with a test pinning the old path, deleted in a follow-up commit once the new one is green on the fixture and the reference logits. That is what makes this reviewable one architecture at a time rather than as a flag day."
    status: pending
  - id: s4-weight-iterator-seam
    content: "STEP 4, and it is the piece llama.cpp does NOT teach and FreeToken does. FreeToken's registry row is `ModelSpec { module, model_cls, parse_config, iter_weights }` (models/register.py), where the last two DEFAULT to the package's own `parse_config`/`iter_weights` but can be overridden per row: `Gemma4GGUFForCausalLM` reuses the whole gemma4 graph with `parse_config='parse_gguf_config'` and `iter_weights='iter_gguf_weights'`. And a per-arch `weight.py` is small because it is a TABLE over one shared reader: `llama/weight.py` is 62 lines, of which the substance is a five-entry `_MERGE_RULES` dict mapping `.q_proj`/`.k_proj`/`.v_proj` onto a fused `.qkv_proj` and `.gate_proj`/`.up_proj` onto `.gate_up_proj`, feeding the shared `models/loader.py` helpers `MergeRule`, `iter_merged_tensors`, `iter_weight_files`, `shard_tensor`. FRINK HAS THE OPPOSITE SHAPE AND HAS ALREADY BEEN BILLED FOR IT: seven GGUF loaders totalling 10,135 lines (`loader.rs` 3316, `kimi_loader.rs` 2207, `kimi_gguf_loader.rs` 1085, `glm52_gguf_loader.rs` 956, `hybrid_gguf_loader.rs` 911, `mla_gguf_loader.rs` 842, `gemma4_gguf_loader.rs` 818), and `freetoken-parity`'s `dedup-sweep-2026-08` records what duplicating across them cost in real defects: `quant_kind_for` drifted into a 21-dtype and a 17-dtype variant, so a DeepSeek-MLA checkpoint at IQ2_XXS was REFUSED OUTRIGHT while the identical quant loaded on the generic path; `load_weight_matrix` drifted so a Q8_0 norm or bias failed to load on five of six architectures; and only `loader.rs` called `register_weight_mmap`, so on Metal every weight of every non-generic architecture was COPIED into its own MTLBuffer instead of aliasing the mapping and paid a 64-page fingerprint on every decode-hot-path cache lookup. That sweep deduplicated the HELPERS. This step deduplicates the LOOP: one shared layer reader, a per-arch `tensors` slot that supplies the name table and the fused-form fallbacks, and the Metal-alias and dtype-widening decisions made once. llama.cpp reached the same place from the other direction and it is worth citing because it is recent: its per-arch tensor-name tables are GONE, `LLM_TENSOR_NAMES` (llama-arch.cpp:381) is now a single flat map so a new architecture edits it only if it introduces a genuinely new tensor kind, and llama-model.cpp:1362-1372 added a generic post-pass for optional per-tensor scale tensors whose comment says the quiet part out loud: 'this avoids having to add scale loading to every architecture'. It also keeps two shared fallback helpers so per-model files stay short, `create_tensor_qkv` (llama-model.h:740, 'try to load merged qkv first, fall back to separate q, k, v') and `create_tensor_gate_up_exps` (llama-model.h:736), which is exactly what frink's `loader.rs:918-996` already does inline for `attn_qkv` and `ffn_gate`/`ffn_up`."
    status: pending
  - id: registration-and-opt-in
    content: "THE ANSWER TO 'how an architecture registers, and how the generic-GQA default becomes OPT-IN'. TODAY registration is one row in `architecture_catalog()` (capability.rs:185-737) and, for a dedicated stack, ALSO a `SelectedEngineKind` variant (engine_factory.rs:31), an arm in `select_engine_kind` (engine_factory.rs:47), an arm in `ensure_generic_decoder` (engine_factory.rs:76), a `ServedEngine` variant (engine_factory.rs:112), a `load_*_engine_from_path` function, and an arm in `frink-server/src/model.rs`. Note the shape of the problem there: `engine_factory.rs` holds FOUR near-identical `load_*_engine_from_path` functions, each with a seven-arm exhaustive match producing an error string, so adding a fifth engine means editing four existing matches. That is llama.cpp's pre-refactor problem in miniature, and llama.cpp's fix is the fix: its three per-arch switches collapsed into ONE table, `llama_model_mapping` (llama-model.cpp:41-318), one `case` per architecture, and the tensor and hparams switches moved into the per-model file behind virtuals invoked from one site each (:1233, :1360, :2413). Target for frink: ONE row, naming the module. THE INVERSION. Today an unrecognised architecture does not fall through by accident, it falls through by CONVENIENCE: `gqa_norm(name)` (capability.rs:131) and `gqa_neox(name)` (capability.rs:145) make admitting an architecture to the generic path a one-word act, and 60 of the 68 were admitted inside two `for n in [...]` loops (capability.rs:189-264). Concretely: change `ArchPath::GenericGqa { rope }` to name an implementation rather than describe a shape, rename the two helpers to `unaudited_gqa_norm`/`unaudited_gqa_neox` pointing at a distinct `PLAIN_GQA_UNAUDITED` static that is behaviourally identical today, and give the audited helper the signature `audited_gqa(name, rope, fixture)` so a row CANNOT claim audited status without naming a test artifact. Then `coverage_report_markdown()` (capability.rs:895, wired to `frink caps` at frink-cli/src/main.rs:746) grows an Audited column and the count of unaudited architectures becomes a number the binary prints and a test pins. `FRINK_STRICT_ARCH=1` refuses `PLAIN_GQA_UNAUDITED` at load. WHETHER THAT BECOMES THE DEFAULT IS NOT THIS PLAN'S CALL, it is `t1-close-the-68`'s, and this plan's job is to give it something structural to flip instead of a comment. THE PRECEDENT IS UNANIMOUS ACROSS ALL THREE REFERENCES AND FRINK IS THE ODD ONE OUT: llama.cpp throws twice for an unknown architecture and has no generic fallback anywhere (llama-model.cpp:334-341 for a name not in the table, llama-model.cpp:315 for an enum with no class, live today for `LLM_ARCH_CLIP` and `LLM_ARCH_GPTJ`); FreeToken's `get_model_spec` raises `ValueError(f'Model architecture {model_architecture} not supported')`; oxillama's registry `get` returns `None`. ONE MORE THING TO TAKE, from FreeToken and oxillama both: an ALIAS IS A CLAIM and needs a reason next to it. Every aliased row in FreeToken's registry carries a comment saying why two architecture strings share a package, and oxillama has `register_with_aliases` (registry.rs:159) plus a test (registry.rs:319) documenting its REFUSAL to alias `starcoder2`, `phi2`, `qwen2` and `internlm2`."
    status: pending
  - id: verification-ladder
    content: "THE ANSWER TO 'what is each step verified by, given a fixture is kilobytes and a real checkpoint is gigabytes'. FIVE RUNGS, four of which already exist, ordered by how much they prove per byte. (1) BIT-IDENTITY AGAINST THE PREVIOUS PATH, and for a refactor this is the decisive one, stronger than any comparison against llama.cpp: `assert_eq!` on the logit vector, not a tolerance. The repo already works this way and already knows when it cannot: `decoder_via_engine_trait_matches_forward_batch_ground_truth` (engine.rs) uses 1e-5 rather than equality and says why in three lines, because batched prefill runs the blocked three-pass softmax while decode keeps the online accumulator. Every step in this plan gets bit-identity as its primary gate and needs no new artifact for it. (2) SYNTHETIC FIXTURES, 23 KB to 103 KB, in `crates/frink-models/tests/fixtures/` (`frink_real_test.gguf` 23,936 bytes, `frink_real_moe_test.gguf` 51,936, `frink_real_mixed_test.gguf` 52,576, `gptoss_tiny.gguf` 95,296, `dots1_tiny.gguf` 103,008), generated by `scripts/make_dots1_fixture.py` and `scripts/make_gptoss_fixture.py`. THE RULE THIS PLAN ADDS is that an `Arch` impl does not land without one, and the enforcement is the `audited_gqa(name, rope, fixture)` signature in `registration-and-opt-in`: the fixture is an argument, so the claim costs something. north-star's `verification-without-downloading-everything` already argues fixtures are the best coverage per byte available; this is where that becomes a rule with somewhere to live. (3) THE GIGABYTES OBJECTION IS SMALLER THAN IT LOOKS. `models/` is already 46 GB across 27 GGUFs and `benchmarks/suite.json` names them, so for Llama-3.2 1B and 3B and 8B, Qwen2.5-1.5B, Qwen3-0.6B, gemma-2-2b, gemma-4-E2B, OLMoE, Qwen1.5-MoE, Phi-4-mini, Mistral-7B, Yi-1.5-6B, TinyLlama and SmolLM2, this is a disk problem that is already solved rather than a download problem. Steps 1 through 3 touch nothing that is not in that list. (4) DIFFERENTIAL AGAINST LLAMA.CPP, `frink parity` (frink-cli/src/main.rs:899) plus `tools/llama_logits.c`, comparing first-token logit distributions on the same file with no knowledge of the architecture. Needed for step 4 onward where a NEW architecture appears and there is no previous frink to be identical to. Not needed for steps 1 through 3, where 'identical to the previous frink' is the stronger claim. (5) SPEED RECEIPTS. `frink bench` with the `host_spec` guard that refuses to merge receipts from different hosts. Every step touching a hot path lands with a before and after on Metal `tg128`, where 8 of 12 rows are ahead and there is something to lose, and on CPU `tg128`. A step that costs measurable tok/s is reverted rather than argued about. AND ACROSS ALL FIVE, SABOTAGE-CHECK: this repo already practises it, commit c5aa646 recorded 'Sabotage-checked: dropping the window in the paged arm fails it, and so does dropping the softcap', and every test added here must be shown to go red when the feature it guards is removed. A guard that cannot fail was the entire mechanism by which five features were lost."
    status: pending
  - id: dispatch-budget
    content: "THE ANSWER TO 'where dispatch cost lands and why it is acceptable there', written as a budget a reviewer can reject a patch against. ACCEPTABLE. PER MODEL, AT LOAD: one `&'static dyn Arch` resolved from the catalog. `resolve_profile` (capability.rs:739) is a linear scan over roughly 160 entries and is called at load only. Free. PER LAYER, PER FORWARD: reading `LayerPlan` fields, one `match` on the attention arm and one on the FFN arm. Same branch count as today's `match (oai, self.config.layer_sliding_window(l))` (decoder.rs:2468) and `match config.ffn_activation` (decoder.rs:3423), against 30 to 80 matmuls of megabytes each. PER TOKEN: at most one virtual call, and only for a Tier-B `Engine`, which is what oxillama does (`Box<dyn ForwardPass>`, one vtable hop per `forward()`, oxillama-arch/src/traits.rs:294) and it is fine at that granularity. NOT ACCEPTABLE, and named so a patch can be rejected on sight. No `Box<dyn>` and no closure inside the per-layer loop. No trait object per op, per head or per element. No architecture string compared below load, a rule `capability.rs` already states in its own module doc ('Architecture names are registry keys only. Hot-path kernels never branch on them'). And NO CHANGE TO THE SHAPE OF THE METAL FUSED STACK: `launch_decode_dense_stack` (frink-metal/src/attn.rs:5928) folds every layer into one command buffer, that is what the 8-of-12 `tg128` wins rest on, and `LayerPlan` may decide WHETHER to take that path but must never be read inside it. THE STRUCTURAL DIFFERENCE FROM LLAMA.CPP HAS TO BE SAID OUT LOUD BECAUSE IT IS WHY THIS PLAN CANNOT SIMPLY COPY IT: llama.cpp pays its per-architecture dispatch exactly once per graph TOPOLOGY CHANGE, not per token, because the graph is data and is cached. `llama_context::process_ubatch` (llama-context.cpp:1321-1390) calls `res->can_reuse(gparams)` and only calls `model.build_graph` (llama-context.cpp:1354) on a miss; in steady-state single-sequence decode the predicate holds and the graph is reused every step, with `n_reused` exported as a perf counter. Everything under `build_arch_graph` is non-virtual overload resolution settled at compile time. Frink executes eagerly and has no graph object, so it has NOTHING to amortise a per-token virtual call over. That is the entire reason the per-architecture unit here produces DATA read by a shared runner rather than CODE that runs the layer. Building a graph IR to copy llama.cpp properly is a rewrite of the execution model and is explicitly out of scope."
    status: pending
  - id: which-reference-fits-and-what-not-to-steal
    content: "THE VERDICT ON FOUR REFERENCE PROJECTS, because 'study these' deserves an answer that says no as often as yes. FREETOKEN FITS BEST FOR THE UNIT, on three grounds. First, its unit maps onto Rust without translation: a package exporting `parse_config`, a model class and `iter_weights` becomes a module with a three-method trait impl, whereas llama.cpp's unit is a class whose third virtual returns a ggml graph and frink has no graph to return. Second, it separates config parsing and weight iteration into their OWN registry slots, which is the seam frink is missing and has already been billed for missing (see `s4-weight-iterator-seam`). Third, frink has ALREADY ACCEPTED THIS PROJECT'S DESIGN ONCE: `frink-edge` is a port of FreeToken's host-side policy, Apache-2.0, credited in `docs/THIRD_PARTY_NOTICES.md`, with the boundary documented in CLAUDE.md, so the licensing question and the vocabulary question are both settled and adopting the model-layer shape from the same upstream is continuity rather than a new dependency on a new project's taste. And the evidence that the shape SCALES BOTH WAYS is the file listing: `llama/` is 5 files and 280 lines, `qwen2/` 278, `qwen3/` 278, `mistral/` 281, `qwen3_moe/` 382, while `deepseek_v4/` is 10 files and 2039 lines with its own `attention.py` (324), `compress.py` (482), `ops.py` (127), `moe.py` (150) and `args.py` (111), and `qwen3_5_moe/` is 2173, all without touching a shared file. That last property is exactly the one frink lacks, where `deepseek_v4_decoder.rs` and `glm_dsa.rs` and `mla.rs` and `kda.rs` and `gdn.rs` sit as siblings of a 6438-line `decoder.rs` they must not edit. LLAMA.CPP STILL WINS ON THE BLOCK VOCABULARY and that is what to take from it: `build_attn` overloaded on the memory-input type, `build_ffn`, `build_moe_ffn` in two forms, `build_norm` switching over three norm types, and `build_qkv` returning a destructurable struct that hides the fused-versus-separate QKV choice (llama-graph.h:1030-1113 and :1145-1249). It also wins on the ANTI-DUPLICATION HABIT: a generic post-pass that 'avoids having to add scale loading to every architecture' (llama-model.cpp:1362-1372), a de-nested flat tensor-name table (llama-arch.cpp:381), and a HOWTO that names the trap explicitly (docs/development/HOWTO-add-model.md:106, 'Check for other places that switch/iterate over every llm_arch value... Missing one of these is a common cause of CI test failures'). WHAT NOT TO STEAL, itemised. From oxillama: its per-arch inlined attention loop (oxillama-arch/src/llama/model.rs:332, roughly 187 lines) and the absence of any shared attention or FFN helper, which is why it pays about 2,100 lines per architecture against llama.cpp's 230 and FreeToken's 280; and `Box<dyn ForwardPass>` as the ONLY seam, which forces every architecture to own its own prefill, batching and KV. Frink already has the better split for Tier A and should keep it. DO steal oxillama's incremental-migration recipe (`engine/mod.rs:1218-1227, :1238`) and its `register_with_aliases` discipline. From Camelid: nothing structural. It is what `decoder.rs` becomes at four times the scale, with `src/inference.rs` at 27,384 lines, `src/gemma4_runtime.rs` at 12,089, no model trait at all, and per-architecture routing done as bare string comparisons scattered inline (`if self.architecture == 'command-r'` at src/runnable/model.rs:1611, :1626, :1663, :1766, :2234). Worth reading only for its typed `Option<PerArchMetadata>` config (src/model.rs:11-100), which frink already does with `GptOssWeights`, and its fail-closed capability routing (src/model.rs:189-260). From omlx: nothing. It is Python, it owns no transformer implementations, and its per-model code is monkeypatches against pinned upstream registered by mutating `sys.modules`. One habit worth keeping: `patches/deepseek_v4/__init__.py:29-34` documents its own deletion condition, and a frink transitional shim should too. From ds4: its BACKEND seam is genuinely clean and its MODEL seam is not. `ds4_gpu.h` is 2603 lines of declarations, 232 entry points over an opaque tensor handle, three independent implementations (Metal 39,615 lines, CUDA 27,666, ROCm), zero function pointers, backend chosen at link time, and `ds4.c` calls through it 3,380 times and never touches a kernel. But the op names are model-shaped (`glm_routed_moe_batch_direct_scalar_q4_tensor`, `dsv4_qkv_rms_norm_rows_kv_rope_tensor`), so adding a third family means adding roughly 50 ops and implementing them two or three times, and with only two families it never had to solve the per-model problem. THE RELEVANCE TO FRINK IS A NAMED RISK RATHER THAN A LESSON: `frink-metal/src/attn.rs` is 9026 lines of exactly these model-shaped fused ops, and it will be the next thing this problem shows up in."
    status: pending
  - id: deepseek4-first-real-target
    content: "THE FIRST ARCHITECTURE TO LAND UNDER THE NEW STRUCTURE, and the argument is that it is the only recent large model where all three references already have the graph AND frink already has most of the kernels. FRINK SIDE, present today: `deepseek_v4_decoder.rs` (922 lines) with `DeepseekV4Engine` behind the existing `Engine` trait (engine.rs), `frink_core::deepseek_v4_attention::{csa_attention, hca_attention}`, `frink_core::csa_hca_compress::{channel_gated_pool, compress_block}`, `hyper_connections.rs` (311), `latent_moe.rs` (436), and a `deepseek_v4_pro()` preset (config.rs:519). REFUSED today at capability.rs:552-554 with the reason 'DeepSeek V4 needs CSA/HCA + mHC assembly; generic GQA Decoder is not valid', and CLAUDE.md is explicit that the preset 'remains a sketch until a real GGUF loader + incremental DSV4 KV land'. REFERENCE SIDE: FreeToken has `models/deepseek_v4/` at 2039 lines across 10 files, including its own `compress.py` (482) and `ops.py` (127), which is a working reference for exactly the CSA/HCA assembly frink is missing; llama.cpp has `src/models/deepseek4.cpp`, which at 1546 lines is its LARGEST per-architecture file (next is `qwen3next.cpp` at 823 and `glm-dsa.cpp` at 810), so there is a second independent graph to check against; and frink's own `.scratch/ds4/` is a C implementation of the same family. THE BENCHMARK ARGUMENT, and it is reported rather than verified here: research on 2026-08-27 found DeepSeek-V4-Flash (`deepseek4`) to be the best candidate for a large real-checkpoint benchmark because ggml-org publishes the GGUF and llama.cpp merged support on 2026-06-29, while most other recent models either fall in frink's unconditional hybrid or recurrent reject families (capability.rs:558-598) or have no llama.cpp baseline to compare against at all. That matters for `measurement-needs-more-than-one-laptop`: every frink benchmark row today is a 1B to 8B model on one M2 Pro, and a real MoE at scale is the row that would test both `t1-out-of-core-execution` and this plan's claim that a Tier-B architecture can land without editing a shared file. SEQUENCING, said plainly: this is NOT step 4. It comes after `s3-arch-trait-and-gpt-oss` proves the seam on an architecture with a 95 KB fixture and an independent reference, because proving a seam on the largest graph in every reference project is how a refactor stops being reviewable. The GO condition is that `arch/gpt_oss.rs` is green and the transitional fallback in step 3 has been deleted."
    status: pending
  - id: not-in-this-plan
    content: "NAMED AND EXCLUDED, so nobody smuggles them in and nobody re-raises them without an argument. (1) UNIFYING `forward_hidden_batch_inner` (decoder.rs:3785) WITH THE TOKEN PATHS. Its rows are consecutive positions rather than independent sequences, its Metal arm set differs, and it currently applies `post_ffn_norm` at seven separate sites because it has roughly seven FFN dispatch arms. It should read `LayerPlan` from step 2 and it should stop re-deriving decorations, but whether it collapses into one body with `forward_token` is genuinely undecided and two runners may be right. (2) WIDENING THE `Engine` TRAIT. Today it carries only `forward_token` (engine.rs:243-252), so a Tier-B architecture gets no batched prefill, no paged KV and no prefix cache, because `frink-server::generate` takes `&Decoder` concretely (generate.rs:200, :307, :388, :741) and `generate_engine` (generate.rs:1203) is the degraded loop. That is a real gap and it is the thing that would make Kimi, GLM-5.2, MLA and Gemma-4 serve at full speed, but it is a serving-path change and mixing it into a model-layer refactor makes both unreviewable. (3) ANYTHING INSIDE `frink-metal` BEYOND THE ELIGIBILITY PREDICATES. Its `attn.rs` is 9026 lines with seven layer-body copies of its own and it is the next place this problem appears, but touching it in the same series as the host paths would mean no step could be verified by bit-identity against a stable other side. (4) A GRAPH IR. Copying llama.cpp's cached-graph model (llama-graph.cpp:1378, llama-context.cpp:1321-1390) would give frink llama.cpp's dispatch story for free, and it is a rewrite of the execution model rather than a reorganisation of it. Recorded because it is the honest answer to why this plan's Tier-A unit produces data rather than code, and because if frink ever does build one, this plan's `LayerPlan` is the thing that would become its node list."
    status: pending
  - id: what-is-inferred-not-confirmed
    content: "WHERE THIS PLAN IS GUESSING, listed so a reader can weigh it. (1) Whether `LayerPlan` can express the six PARALLEL-RESIDUAL families without a second executor. `command-r`, `cohere2`, `cohere2moe`, `falcon`, `gptneox`, `phi2` and `plamo` are refused today at capability.rs:400-427 because llama.cpp 'feeds both branches the same normed input and sums inpL + attn_out + ffn_out once', and this plan assumes that becomes one `ResidualForm` field. That is inferred from capability.rs's own citations of llama.cpp's graphs, not verified against a checkpoint, and if it is wrong those seven architectures are Tier B rather than Tier A. It matters because they would be the first architectures this refactor makes NEWLY RUNNABLE rather than merely tidier, which is the difference between optimising and reorganising at the product level. (2) Whether the Metal fused stack can be GATED from `LayerPlan` without losing single-command-buffer fusion. `launch_decode_dense_stack` (frink-metal/src/attn.rs:5928) takes a hand-built slice of per-layer launch descriptors whose `post_attn_norm`/`post_ffn_norm` fields are at :5902-5903, and building that slice from a plan is probably mechanical. 'Probably' is the honest word and step 2 should prove it before step 3 depends on it. (3) Whether `ArchProfile` can keep `#[derive(Debug, Clone, Copy, PartialEq, Eq)]` (capability.rs:98) once it carries a `&'static dyn Arch` field. It cannot derive `PartialEq`, `Eq` or `Debug` over a trait object, so those become hand-written impls keyed on `imp.name()`, or the implementation moves to a parallel table keyed by architecture name. Undecided, and the kind of detail that stalls an implementation if it is discovered late. (4) The line-count target. llama.cpp is 32,514 lines over 140 files, about 232 per architecture, with block-level helpers. FreeToken is 280 for a simple architecture with OP-level helpers. oxillama is about 2,110 with neither. Frink should land nearer the first two, but NO NUMBER IS BEING PROMISED until `arch/plain_gqa.rs` and `arch/gpt_oss.rs` exist and can be measured. (5) How many decorations are actually lost. Six are known. The sixth was found by grepping ONE feature across four paths, and nobody has done that for the other fifteen or for the seven Metal launches. `decoration-audit` is first in the order precisely because the answer is currently unknown, and it may well be larger than six."
    status: pending
isProject: false
---

# Model layer reorg: make adding an architecture a new file

> This is the plan north-star's `t1-scale-the-model-layer` points at as
> `model-registry-reorg.md`. That file was never written.

## The number, and what it is made of

`crates/frink-models/src/decoder.rs` was **6438 lines** when this was
measured. Code ran to 4842; the rest was its own test module. **It is
6702 today**, and `frink-metal/src/attn.rs` is 8860 against the 9026
below. The line numbers in the tables that follow are from the original
measurement and have moved; the shape they describe has not. The
direction of travel is the argument for this plan, not against it.

Inside it, four host functions each contain a complete decoder layer
written out longhand:

| function | line | shape |
|---|---|---|
| `forward_token` CPU arm | 2394-2531 | one row, contiguous KV |
| `forward_token_paged` | 2585 | one row, paged KV |
| `forward_hidden_batch_inner` | 3785 | n consecutive positions, two arms |
| `forward_multi_seq_kv` | 4625 | n independent sequences |

`crates/frink-metal/src/attn.rs` is **9026 lines** and holds seven
more, one per fused launch (`attn.rs:5311, :5439, :5657, :5928, :6890,
:7082, :7238`).

**Eleven copies of one decoder layer, across two crates.**
`post_ffn_norm` is applied at seven separate sites inside
`forward_hidden_batch_inner` alone.

## What that costs, on the record and newly found

Already recorded: the paged decode loop had **lost five model
features** by being a copy of the contiguous one, three of them wrong
on CPU too. PR #22 hit the same shape from the other side and fixed it
by making `forward_multi_seq` take a `MultiSeqKv` parameter rather than
growing a paged twin (commit `c5aa646`; the reasoning is in
`decoder.rs:444-453`).

Found while writing this plan, and it is the argument in miniature:

- `attention_scale` is applied at `decoder.rs:2459` and
  `decoder.rs:2699`. **Nowhere else.** Batched prefill and the
  multi-sequence path do not apply it.
- It is unreachable today only because `loader.rs:458` hardcodes
  `let attention_scale = None;`, and `unsupported_scaling_keys` refuses
  a file carrying a non-noop `*.attention.scale`.
- The guard test that exists,
  `the_paged_path_keeps_every_per_layer_feature_the_contiguous_one_applies`
  (`decoder.rs:5249`), sets `attention_scale = Some(0.37)` and compares
  the two paths that **agree**, never the two that do not.

A sixth gap, latent behind a loader refusal, invisible to the test
written specifically to catch this class. That is what "nothing
notices" looks like.

## Two references, one unit each

Neither replaces the other.

| | FreeToken | llama.cpp | oxillama |
|---|---|---|---|
| per-arch unit | package (5-10 files) | one `.cpp` + a decl | directory (5-6 files) |
| simple arch | `llama/` **280 lines** | `llama.cpp` **250** | `llama/` **2845** |
| complex arch | `deepseek_v4/` **2039** | `deepseek4.cpp` **1546** | `mamba2/` **3364** |
| per-arch average | ~800 | **~232** | **~2110** |
| shared vocabulary | OP objects, `layers/` 1623 | **block builders**, `build_attn`/`build_ffn` | primitives only |
| registration | one dict row | enum + name + decl + `case` | one `register()` line |
| unknown arch | `ValueError` | **throws twice** | `None` |
| config + weights | **own registry slots** | own virtuals | inside the arch |

oxillama is the control experiment: same language, same problem, split
per-arch **without** block helpers, and it pays 2,110 lines per
architecture because every arch inlines its own attention loop
(`oxillama-arch/src/llama/model.rs:332`, ~187 lines).

**So: vocabulary first, split second.** Splitting before the block
helpers exist lands on oxillama's number, not llama.cpp's.

## The unit, in frink terms

**Tier A**, the common case, `crates/frink-models/src/arch/<name>.rs`,
one ZST implementing three methods:

```
hparams(&self, &ShardedGguf, &mut ModelConfig)      // load_arch_hparams / parse_config
tensors(&self, &ShardedGguf, layer, &mut LayerBuild) // load_arch_tensors / iter_weights
layer_plan(&self, &ModelConfig, layer, &LayerWeights) -> LayerPlan
```

The third diverges from llama.cpp on purpose. `build_arch_graph`
returns a ggml graph that `llama_context` then **caches and reuses
across tokens** (`llama-graph.cpp:1378`, `llama-context.cpp:1354`).
Frink executes eagerly and has no graph object, so there is nothing to
amortise a per-token virtual call over. `layer_plan` returns **data
built once at load** that one shared runner reads.

**Tier B**, a genuinely different graph, is the **existing** `Engine`
trait (`engine.rs:243`). Kimi, GLM-5.2, DeepSeek-V4, MLA and Gemma-4
already live there. This plan proposes no change to its shape.

## The order

```
0  decoration audit          one table; it found gap six, and gap count is unknown
1  one attn_block            forward_token + forward_token_paged collapse; a MOVE
2  LayerPlan                 built at load; finishes the dead ExecutionPlan
3  trait Arch + arch/        plain_gqa and gpt_oss, with oxillama's fallback recipe
4  weight-iterator seam      one loader, per-arch name tables (7 loaders, 10,135 lines)
5  registry inversion        generic GQA stops being the shortest thing to write
6  Metal stacks read LayerPlan
   ---- then ----
   deepseek4                 the first Tier-B arch under the new structure
```

Steps 1 through 3 are separately landable and separately verifiable.
Step 1 touches three call sites and adds no types.

## Verification, ranked by proof per byte

1. **Bit-identity against the previous path.** `assert_eq!` on logits,
   not a tolerance. Needs no new artifact. Primary gate for steps 1-3.
2. **Synthetic fixtures**, 23-103 KB, already in
   `crates/frink-models/tests/fixtures/`. New rule: an `Arch` impl
   lands with one, enforced by making the fixture an **argument** to
   the registry helper.
3. **Real checkpoints already on disk.** `models/` is 46 GB, 27 GGUFs,
   named in `benchmarks/suite.json`. Steps 1-3 touch nothing outside it.
4. **`frink parity` + `tools/llama_logits.c`.** Needed from step 4,
   where a new architecture has no previous frink to match.
5. **`frink bench` receipts** on Metal `tg128` and CPU `tg128`, every
   step that touches a hot path.

And **sabotage-check everything**, as `c5aa646` already did. A guard
that cannot fail is how five features went missing.

## The dispatch budget

| where | cost | verdict |
|---|---|---|
| per model, at load | one `&'static dyn Arch` | free |
| per layer, per forward | `LayerPlan` field reads, two `match`es | same as today |
| per token | one vtable hop, Tier B only | fine (oxillama does this) |
| **inside the layer loop** | `Box<dyn>`, closures | **rejected** |
| **per op / head / element** | any trait object | **rejected** |
| **below load** | any arch string compare | **rejected** (`capability.rs:10-12`) |
| **inside `launch_decode_dense_stack`** | anything at all | **rejected** |

Metal is at parity and 8 of 12 decode rows are ahead of llama.cpp.
`launch_decode_dense_stack` (`frink-metal/src/attn.rs:5928`) folding
every layer into one command buffer is what those rows rest on.
`LayerPlan` decides **whether** to take that path. It is never read
inside it.

## Where this plan is guessing

- Whether the seven parallel-residual families collapse into one
  `ResidualForm` field. Inferred from `capability.rs:400-427`, not
  verified. They would be the first architectures this refactor makes
  **newly runnable** rather than merely tidier.
- Whether the Metal fused stack can be gated from `LayerPlan` without
  losing its single command buffer. Probably mechanical. "Probably" is
  the honest word.
- Whether `ArchProfile` keeps its derives with a `dyn` field
  (`capability.rs:98`). It cannot derive `PartialEq`/`Eq`/`Debug` over
  a trait object.
- How many decorations are actually lost. **Six known.** The sixth was
  found by grepping one feature across four paths. Nobody has done that
  for the other fifteen, or for the seven Metal launches. That is why
  the audit is step 0.
